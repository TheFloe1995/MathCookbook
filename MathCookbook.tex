\documentclass[%
	pdftex,
	12pt,								% font size
	parskip=half,				% vertical line spacing
	headheight = 12pt,	% height of header
	headsepline,				% line after header
	footsepline,				% line before footer
	footheight = 16pt,	% height of footer
	DIV=calc,						% calc print space
	BCOR=8mm,						% bind correction left
	headinclude=false,	% include of header in print space
	footinclude=false,	% include of footer on print space
]{scrreprt}						% for long documents

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

\title{A Math Cookbook}
\author{Florian \"Olsner\\florian.oelsner@tum.de}

\usepackage{amsmath}
\usepackage{commath}
\usepackage[ngerman, english]{babel}
\usepackage[margin=2cm,foot=1cm]{geometry}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{bm}
\usepackage{mathtools}

\begin{document}
\maketitle
\newpage

 \chapter{Some useful stuff}
 \section{Taylor Expansion}
 \begin{equation}
 	\sum_{n=0}^{\infty} \frac{f^{(n)(a)}}{n!}(x - a)^n = f(a) + f'(a) (x-a) + \frac{f''(a)}{2}(x-a)^2 ...
 \end{equation}

 \section{Newton's Method}
 \begin{align}
 		x_{n+1} &= x_n + \frac{-f'(x_n)}{f''(x_n)} \\
		\bm{x}_{n+1} &= \bm{x}_n - \gamma[\bm{H} f(\bm{x}_n)]^{-1} \nabla f(\bm{x_n})
 \end{align}

 \section{Newton's Method}
 \begin{align}
 		x_{n+1} &= x_n + \frac{-f'(x_n)}{f''(x_n)} \\
		\bm{x}_{n+1} &= \bm{x}_n - \gamma[\bm{H} f(\bm{x}_n)]^{-1} \nabla f(\bm{x_n})
 \end{align}

\section{Matrix Rank}
For any $n \times m$ matrix $\bm{A}$:
\begin{align}
	\text{rank}(\bm{A}) &= \text{rank}(\bm{A}^T) = \text{row-rank}(\bm{A}) = \text{col-rank}(\bm{A})\\
	& \leq \min(m, n)
\end{align}
If $\bm{A}$ is real valued:
\begin{equation}
	\text{rank}(\bm{A}) = \text{rank}(\bm{A}^T \bm{A}) = \text{rank}(\bm{A} \bm{A}^T)
\end{equation}



\end{document}
